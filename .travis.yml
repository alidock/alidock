dist: trusty
language: python
python:
  - "2.7"
  - "3.7-dev"
#services:
#  - docker
#cache:
#  directories:
#    - $HOME/sw/TARS
install: |
  set -e
  set -o pipefail
  for PKG in pylint argparse requests PyYaml docker Jinja2; do
    pip install "$PKG"
  done
script: |
  set -e
  set -o pipefail
  ci/run-tests.sh
#after_success:
#  - bash <(curl -s https://codecov.io/bash)
#env:
#  global:
#    - secure: "..."
#    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true  # speeds up installation of html-proofer
